# NextMail 开发记录

## 2026-09-12

- [x] 邮件头同步依据 `BODYSTRUCTURE` 按相同 section 路径合并取得有界文字预览；解析失败、无纯文本正文或结构兼容降级时使用固定单行占位符。新邮件进入前端列表前始终已有非空预览，完整正文与附件继续按需下载。
- [x] 将中英文翻译资源按业务域拆分并保持既有翻译 key，将前端 IPC 调用按业务域拆分并由 `src/app/api.ts` 提供稳定统一入口；默认主题改为跟随系统。
- [x] 全局图标默认使用主题色，并为主操作与状态控件保留清晰对比；标题栏改为无渐变半透明表面与紧凑圆角窗口按钮。

## 2026-09-11

- [ ] 调查并修复大型邮箱同步中的前端缓存积累与刷新放大：清理虚拟列表失效测量和 DOM 观察，固定条数换页正确更新取键，精准失效单封正文并回收离开后的正文/EML/Mutation 缓存；Rust 进度发送前限流，队列溢出复用请求并完整清理卸载状态。已补 2 万次轮换与 1 万次突发等自动回归，待真实大型邮箱 Windows 内存走势验收。
- [ ] 统一后端错误分类、日志脱敏和前端提示：覆盖基础设施错误转换，保留安全原因、数字错误码及来源；补齐中英文错误文案，保留原因与稳定错误码，合并后台配置保存的重复失败并禁止永久错误自动重试。日志增加有界队列、限流、14 文件保留与退出刷新；补 Windows 配置文件占用、损坏配置、日志脱敏和重复提示测试，实际账户保存失败原因须用新日志确认。

- [x] 移除邮件列表行在点击未读邮件并产生待同步操作后显示的云端同步图标，后台已读操作与持久化重放保持不变。

## 2026-09-10

- [x] 将邮件头同步扩展为三条可复用健康会话与一条独立交互会话，保持每条命令最多 20 UID，并用共享动态批次队列消除静态 worker 等待；前端保留逐封到达事件，以最多 256 封的有界队列按动画帧逐封写入首页，联系人和邮箱刷新合并限流，邮件列表改用仅挂载可视行及 overscan 的虚拟列表。
- [x] 修复部分邮箱返回非标准 `BODYSTRUCTURE` 后污染 IMAP 会话并在下一文件夹误报不支持 UID 的循环失败：保留已落库邮件头，立即废弃连接池并有界重连，当前进程内对受影响账户自动禁用结构化抓取；正文预取及打开邮件改走新连接上的完整邮件回退，避免坏连接反复重试。

## 2026-09-09

- [x] 新增本次进程有效的隐藏演示模式：关于页 NextMail 标题连续点击 11 次后确认进入，关闭设置及其他非编辑业务窗口、重载主界面并切换为统一使用 `next-mail.app` 展示身份的双语虚构商务账户、联系人和 12 封不同类型邮件；内存邮件操作与真实 IPC 双层隔离，账户管理与文件访问不可用，新建邮件按钮点击静默忽略，并暂停后续真实收发调度。Ctrl+L / Cmd+L 呼出语言选择，原生 accelerator 覆盖邮件 iframe 焦点；完全退出重开恢复真实数据与偏好。

## 2026-09-08

- [x] 新增账户隔离的联系人分组；联系人页加号后提供独立管理对话框，支持创建、改名、搜索/分页选择成员、仅看已选和删除，保护未保存编辑与并发修改。Composer 及模板的收件人/抄送/密送补全支持整组加入与当前栏地址去重。

## 2026-09-06

- [x] 将 HTML 正文的滚动条移入右侧原有留白，以受控 CSS 呈现与邮件列表一致的 6px 主题滑块；有溢出时始终显示，悬停加深。保留 iframe 安全隔离与左右各 16px 总留白，正文和图片宽度不因滚动条出现而变化。
- [x] 将文件夹与邮件列表滑块在各自留白内居中，保持列表与折叠图标位置稳定；分栏拖拽命中宽度从 12px 收窄至 6px，与滑块操作区分离，并取消隐藏折叠按钮的鼠标命中。

## 2026-09-02

- [x] 将邮件正文图片限制在阅读视口宽度内，超宽时保持比例缩小，不改变图片点击行为。
- [x] 邮件详情右上角的更多菜单和邮件列表右键菜单均可将原始邮件另存为 EML，邮件正文继续禁用原生右键菜单。

## 2026-08-27

- [x] 统一 Composer 富文本编辑、HTML 预览与实际发件的正文样式，沿用当前紧凑的 `14px / 1.2` 默认排版并随 HTML 正文发送；显式格式继续覆盖默认值，引用原文保持独立样式。

## 2026-08-23

- [x] 读取常见邮件优先级头部并归一保存为高优先级状态，在邮件列表和阅读详情标题前显示可访问的 `❗` 标记。
- [x] 收敛邮件搜索到主题、收发件人和正文，短字符改为按时间索引在目标范围内逐页查找；搜索结果提供当前文件夹与当前账户全局范围切换，并保留真实来源文件夹供后续操作。
- [x] 邮件中的“编辑联系人”改为直接弹出共享联系人编辑器，通过窄联系人摘要查询取得最新 revision，不再切换联系人页面或等待最近往来邮件查询。
- [x] 修复联系人右键菜单操作冒泡到邮件行并取消邮件选择的问题，打开联系人编辑器时保留当前邮件详情。
- [x] 正文尚未收取时暂不展示附件投影，等待正文加载与 CID 内联判定完成后再显示真实附件。
- [x] 按系统、账户、窗口、邮件和 Composer 拆分 Tauri Command 模块，保留原有 Command 名称与 IPC 注册路径。
- [x] 外置大型 Rust 单元测试，并将存储层整理为 Repository 组合、数据库生命周期、邮件读取和公共辅助模块，复用统一的事务、时间与错误转换实现。

## 2026-08-21

- [x] 修正 HTML-only 邮件的文本提取逻辑，只从 `<body>` 生成纯文本与列表预览，不再混入 `<head>` 中的标题。
- [x] 移除阅读区标题下的“等待同步”；文件夹同步进度只在手动收取时显示，启动与定时同步继续静默运行。
- [x] 新增账号级“未读邮件”滚动列表入口并作为默认视图，支持跨文件夹查看及一键全部标为已读；邮件行使用可交互的实心/空心圆点切换未读状态。
- [x] 扩大文件夹行的点击区域，将叶子文件夹左侧展开占位也纳入选择；缩窄展开槽，并让“邮箱文件夹”标题、未读入口与根级文件夹图标左侧对齐。
- [x] 新收取的 BASE64 附件按 MIME 编码大小乘 `0.730769` 估算展示大小，附件实际下载后继续以真实字节数覆盖；不迁移历史附件。
- [x] 滚动条改为仅在指针移入滚动区时显示，不再因列表内部保有键盘焦点而持续可见。
- [x] 文件夹与邮件列表的上下方向键改为选择并聚焦相邻项目，不再触发列表默认滚动。
- [x] 收紧邮件已读/未读按钮的 hover 反馈，只在状态圆点周围显示小范围淡色环，同时保留原点击区域。
- [x] 修复重复点击当前文件夹会清空邮件选中态和阅读区的问题；只有切换到不同文件夹时才重置文件夹内状态。
- [x] 在“邮箱文件夹”上方新增“收藏夹”：真实收藏文件夹优先排列，默认包含收件箱，其后显示不可移除的账号级“星标邮件”和“未读邮件”；真实文件夹可通过右键菜单添加或移除收藏，收藏状态按账户持久化，同一文件夹只高亮实际点击的收藏入口或文件夹树入口。
- [x] 修复“未读邮件”视图中打开邮件后立即消失的问题：当前视图保留并更新已读状态，离开后再次进入才按本地数据库刷新移除。
- [x] 按账户保存最后选中的真实或虚拟文件夹，下次启动及切回账户时恢复；目标不存在时优先回退到真实收件箱。
