# 0032 功能路线重排

日期：2026-07-20

## 变更

- 从第八阶段删除未实施的 P2/P3 性能、工具链和清理候选；该阶段以三个已验收批次结束。
- 将第九阶段调整为模板与签名，第十阶段调整为邮件阅读与回复体验，第十一阶段保留搜索、会话与桌面集成。
- 新增三个 `iterations/` 阶段文档并明确标记“规划中”或“未排期”，后续不使用独立 next 计划。
- 第十阶段记录 HTML/CSS 保真、受控外链、深色模式、完整可编辑回复/转发和默认签名布局目标。
- POP3、Google/Microsoft OAuth、发布硬化和功能完成后的全局优化保留为设想，但不进入当前排期。

## 安全说明

- ADR 0002 在第十阶段实施前继续有效；当前不会移除 sandbox iframe 或放宽 scripts、forms、same-origin、top-navigation、任意文件和任意网络权限。
- “保留原始 HTML/CSS”指避免不必要的排版降级，不表示把未经处理的不可信内容插入主 React/Composer DOM。
- 如果第十阶段选择新渲染隔离模型，必须先以新 ADR、威胁模型和恶意语料验证取代现有决策。

## 行为影响

本次只调整文档路线，没有修改应用代码、依赖、Command、DTO、SQLite、Capability 或用户可见行为。

## 验证

所有本地 Markdown 相对链接可解析，`git diff --check` 通过。此次只有文档变更，不重复运行应用测试或 Tauri bundle。
