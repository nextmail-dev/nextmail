# 变更 0059：项目状态与跨会话交接文档收敛

日期：2026-08-01

## 变更

- 新增 `docs/handoff.md`，集中记录第二十一阶段验收后的产品能力、未实现范围、技术栈、仓库结构、架构与安全边界、同步模型、数据/配置/日志、窗口/Capability、Composer/草稿/发件语义、验证基线和可复制的新会话提示词。
- 更新根 README，使当前能力反映头部优先/可选全文同步、文件夹管理、安全 CID/data/BMP 阅读、本地 FTS、模板签名、显式草稿、独立窗口和自有通知；移除已经失效的 IMAP IDLE 与“尚未实现”描述。
- 修正技术参考中的 SQLite schema metadata 版本为 24，并补全 `definition-*`、独立邮件预览、窗口状态类别与当前事件清单；明确第二十一阶段已验收且当前没有活动阶段。
- 更新架构基线和平台窗口 ADR，补全模板/签名定义窗口、邮件预览窗口的最小 Capability、隐藏至就绪和窗口状态映射。
- 修订持久化发件、多账户、Composer HTML、搜索和显式同步 ADR 中已经被后续阶段替代的句子，保留历史决定同时明确当前 Sent/Drafts、会话预算、签名节点、手动搜索和可选全文同步事实。
- 更新总体计划和开发指南，明确第十一阶段只有搜索部分完成、当前无实施中批次、SQLite schema v24 与目录标记 v1 是两个独立版本，并把交接文档加入新会话阅读顺序。
- 修正第三阶段仍写“等待验收”和第十一阶段仍写“实施中”的历史状态；保留未完成能力但取消自动续做含义。

## 边界

- 本次只整理文档，不修改代码、迁移、依赖、权限、安全策略或产品行为。
- 未创建第 22 阶段，也未把 POP3、OAuth、会话、托盘、发布或全局优化重新排期。
- 未执行 Tauri bundle，未清理 `dist` 或 `src-tauri/target`，未提交或推送。

## 验证

- 检查仓库内 Markdown 相对链接均指向存在的文件或目录。
- `git diff --check` 通过。
- 因为没有产品代码、配置或依赖变化，本次不重复运行前端/Rust 构建与测试；最后一次已验收代码基线仍以 `docs/changes/0058-pre-release-bug-fixes.md` 的记录为准。
