# 第二十三阶段：长期文档收敛

## 状态

已验收（2026-08-01；用户要求恢复被取代的 ADR 0001 后提交并推送）。

## 目标

删除已经完成使命的跨会话交接和阶段流水账，把当前有效事实、开发细则、工程约定与长期决策整理成少量稳定文档，降低重复、过期快照和互相引用造成的维护成本。

## 已确认范围

- 新增 `docs/project.md` 作为新会话唯一必读入口，集中保存当前产品状态、技术事实、仓库地图、数据/同步/窗口/编辑器语义、开发细则、工程约定、已知限制与后续设想。
- 保留 `docs/iterations/`，把每个阶段的范围、实施变更摘要、验证与验收放在一起，方便按迭代查找历史行为。
- 保留仍有长期解释价值的 ADR 与 `third-party-notices.md`，为 ADR 增加状态索引。
- 删除旧会话交接、独立变更流水账，以及已被新文档吸收的文档索引、技术参考、架构基线、开发指南和总体计划；仍需查找的变更摘要归并到对应 iteration。
- 清理仓库 Markdown 中指向被删除文档的链接或说明。

## 非目标

- 不修改 React、Rust、依赖、数据库、Capability、协议、安全策略或产品行为。
- 不改写 Git 历史；逐提交细节继续由 Git 记录承担。
- 不 commit、push、创建 tag、运行 Tauri bundle 或发布 Release。

## 验证

- 保留的根 README、项目手册、iteration 索引与 23 份阶段记录、ADR 和第三方说明共 44 个 Markdown 文件，本地 Markdown/HTML 链接全部可解析。
- 新增 iteration 总索引；22 份历史 iteration 均增加“迭代变更摘要”，覆盖原独立 change 中需要按阶段查找的功能变更；第 23 阶段使用本文件记录本次收敛。
- 仓库不再存在或链接旧会话交接、独立 change、重复技术参考、架构基线、开发指南和总体计划。
- `git diff --check` 通过；只有 Windows 工作区既有 LF/CRLF 提示，没有空白错误。
- 本阶段只修改文档，没有运行产品测试、前端构建、Rust 检查或 Tauri bundle。

## 迭代变更摘要

- 新增 `docs/project.md` 单一长期入口，保留 iterations、当前 ADR 与被取代的 ADR 0001，归并独立 change 摘要并删除旧交接和重复参考文档。
