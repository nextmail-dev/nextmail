# Changelog

NextMail 的版本变更记录。版本号遵循 [Semantic Versioning](https://semver.org/)。

## [0.7.0] - 2026-08-15

### ⚠️ 重要：此版本不支持从旧版本升级

0.7.0 重建了数据库基线，**不再支持从 0.6.x 及更早版本升级**。安装并启动后如提示迁移失败，请删除旧数据目录并重新初始化（重新选择数据目录、添加账户与偏好设置）。旧数据不会被自动删除，删除前请自行备份需要保留的内容。

### 新增

- 设置-通用新增“开机自动启动”开关（默认关闭），按系统机制（Windows 注册表 Run 键 / macOS LaunchAgent）持久化。
- Windows 下同一应用标识只能运行一个实例：再次点击图标会显示并聚焦既有主窗口（包括从托盘隐藏状态恢复）。
- 初次同步先建立完整文件夹树再逐文件夹同步：侧栏立即显示全部目录结构，邮件随后逐封进入。

### 修复

- 网易系邮箱（163/126/188）登录后发送 RFC 2971 IMAP ID 身份标识，修复服务器以 `Unsafe Login` 拒绝邮箱操作、同步反复失败的问题。
- 带说明文字的复选框 hover/焦点交互区扩展为整行宽度。
- 修复初始化向导顶部内容与滚动条被固定标题栏遮挡的问题。

## [0.6.7] - 2026-08-15

### 修复

- 修复 Windows 0.6.5 及更早版本建的数据库升级后启动报 `data_directory.database_migration_failed` 的问题：启动时把历史 CRLF 基准的迁移校验和幂等改写为 LF 值后再校验。
- 修复 QQ 邮箱退信报告邮件导致整账户同步反复失败、邮件无法入库的问题：头部与 `BODYSTRUCTURE` 拆分为两条 FETCH，结构解析失败时按批降级跳过并保留已收头部，不再中断同步。

## [0.6.6] - 2026-08-15

### 修复

- 修复一次性同步大量邮件时前端逐渐卡顿、拖拽分隔条不跟手的问题：同步进度与邮件到达事件按 100ms 合并写入缓存，进度订阅收敛到侧栏，各主要面板 memo 化并稳定回调引用。
- 新增 `.gitattributes` 将迁移 SQL 行尾固定为 LF，修复 Windows CRLF 检出导致 sqlx 迁移校验和不一致、不同机构建的版本互相打不开同一数据库的问题。

## [0.6.5] - 2026-08-15

### 变更

- 大型邮箱的邮件头同步改为每条 IMAP 命令最多获取 20 个 UID，并逐封解析、落库和显示；单账户使用两条同步连接并保留一条动态交互连接。
- 已读、星标、移动等持久化待办使用独立运行循环，完整同步期间仍可执行；同步进度直接通过事件逐封更新，不再逐封发起 IPC 查询。
- 正文选择性下载会获取 HTML 实际引用且可能被误标为 `application/octet-stream` 的 CID 图片，同时保留既有类型、魔数和大小校验。

### 修复

- 修复高频同步进度查询导致前端逐渐卡顿并最终 OOM，以及合并进度后数字跳变的问题。
- 修复打开邮件或写入待办与同步落库竞争 SQLite 写锁时导致完整同步停止的问题。
- 修复 `BODYSTRUCTURE` 中 RFC 2231 分段或 RFC 2047 编码附件名显示为原始 encoded-word 的问题。

## [0.6.0] - 2026-08-14

### 新增

- Composer 支持将系统文件直接拖入窗口并添加为附件。
- macOS 关闭全部可见窗口后，点击 Dock 图标可重新显示并聚焦主窗口。

### 变更

- 长 Select、DropdownMenu 和 ContextMenu 自动限制在当前视界内并提供滚动条。
- 邮件与联系人列表支持再次单击取消单选；统一联系人选中标识、文件夹排序落点和通知从属禁用态。
- Composer 签名上方统一显示细分隔线；未接入应用右键操作的区域不再显示 WebView 原生菜单。

### 修复

- 修复草稿附件删除 SQL 参数绑定错位导致 Composer 无法删除附件的问题。

## [0.5.0] - 2026-08-14

### 新增

- IMAP 邮件头同步同时保存 `BODYSTRUCTURE`；正文、CID 资源和普通附件按 MIME section 选择性获取，复杂结构保留完整 EML 回退。
- 附件支持按需下载和紧凑文件块展示；右键菜单提供打开、打开文件夹和另存为，并预留禁用的“打开方式”入口。

### 变更

- 删除“下载附件后自动打开”偏好，单击附件统一下载后使用系统默认应用打开。

### 修复

- 修复文件夹操作对话框关闭后残留交互遮罩的问题。
- 修复移动文件夹目标下拉列表被对话框遮挡、无法选择的问题。

## [0.4.1] - 2026-08-13

### 新增

- 联系人、邮件发件人和账户入口统一使用主题渐变身份头像，并修复账户头像阴影被文本裁切容器截断的问题。

### 变更

- 浅色模式下将邮件中的不透明纯白背景映射为阅读面板表面色，近白色、品牌底色、图片和视频保持原样。
- 单账户 IMAP 会话上限提升至 6，其中完整同步最多使用 4 条，为交互操作保留 2 条。

### 修复

- 正文回填遇到服务器端已消失的单封邮件时跳过该邮件，不再导致整条账户同步失败并离线。
- Windows 退出时先显式销毁全部 WebView 窗口，避免 Chromium 报告 `Chrome_WidgetWin_0` 清理错误 1412。

## [0.4.0] - 2026-08-11

### 新增

- 为缺少原生深色样式的 HTML 邮件增加 Smart Inversion：静态计算安全 CSS cascade，保留色相与饱和度并校正文稿、背景和边框对比度。
- 识别邮件作者提供的标准及 Outlook 深色适配信号，避免重复转换。

### 变更

- 重构桌面 UI 表面层级、标题栏边界、列表密度、高光与阴影，并统一主窗口和独立业务窗口视觉语言。
- App 深色模式改用零色度中性灰阶；邮件默认白色表面在深色模式下与阅读面板保持一致。
- 统一覆盖式滚动条、桌面默认指针和克制的键盘焦点反馈。

### 修复

- 保留传统 HTML 邮件的 `hr[color]` 和表格 `bordercolor`，避免浅色模式回落到 UA 默认样式或深色模式下边框不可见。
- 改善高亮底色与作者文字色的深色适配，并保持图片和视频内容不变。

## [0.3.1] - 2026-08-11

### 变更

- 优化 Composer 地址栏：默认显示抄送，使用独立密送按钮，统一标签字号、对齐方式和细分隔线。
- 联系人候选支持方向键选择与回车确认；富文本正文中的 Tab 改为插入制表空格。
- 调整邮件详情标题字号并移除 primary 按钮悬停上移动画。
- 回复与转发使用各自明确的原始邮件标题，并将标题与分隔线置于同一行。

### 修复

- 修复 Windows 非整数缩放下 Composer 地址栏分隔线粗细交替的问题。

## [0.3.0] - 2026-08-09

### 新增

- 检测到新版本时使用单例独立窗口展示更新，并安全渲染受限 Markdown 更新说明。
- 绘制并启用了全新的图标。

### 修复

- 修复发布工作流生成的 updater 清单使用 GitHub Assets API URL 的问题，并从规范化直连清单派生大陆代理清单。
- 修复窗口内对话框和发送进度层未完整覆盖顶部拖动区域的问题，统一全局模态层级与拖动命中边界。
- 调整设置页复选项的 hover 与点击区域，以复选框、标题和说明组成内容包裹式圆角交互区域。
- 兼容先按 RFC 2231 风格分段、拼接后才形成完整 RFC 2047 encoded-word 的附件文件名，同时保留标准百分号编码行为。

## [0.2.3] - 2026-08-09

### 修复

- 适配 Geo API 包含 `ip`、`type` 与 `country_code` 的新响应格式。
- Updater 同时配置 GitHub 直连与 NextMail 反代清单，按地区选择主地址并在失败时尝试备用地址。

## [0.2.2] - 2026-08-09

### 新增

- 增加 Windows、macOS 与 Linux 系统托盘，提供显示主界面、设置和退出操作。
- 增加设备级关闭偏好，可选择最小化到托盘、退出前询问及记住关闭选择。
- 增加签名自动更新，在设置“关于”中支持启动检查、手动检测、下载安装和重启。
- 中国大陆更新自动使用 NextMail GitHub 反代清单与下载地址，其他地区及定位失败时回退 GitHub 直连。

### 修复

- 修复首次设置界面顶部重复预留标题栏高度形成的空白区域。
- 修复开发模式下 updater 缺少基础配置时导致应用初始化失败的问题。

### 变更

- 按内容、行为、托盘图标等职责重新整理设置项分组。
- 发布流程增加 updater 产物签名，以及直连 `latest.json` 和大陆代理 `latest-cn.json` 清单。

## [0.2.1] - 2026-08-09

### 新增

- 邮件列表和联系人列表支持 Ctrl/Cmd、Shift 范围多选，以及针对当前选择的批量右键操作。
- 邮件支持批量已读、星标、移动、复制、归档和删除；联系人支持账户内批量直接删除。
- 联系人删除后，后续收信再次发现同邮箱时可以自动重建联系人。
- 附件卡片增加“在文件夹中显示”，由 Rust 复用账户校验和安全缓存后定位文件。

### 修复

- 收件人和抄送地址默认限制为一行，仅在真实溢出时显示展开/收起按钮，避免挤压正文。
- 正文按需加载时只显示 spinner，移除阶段文案和进度条；失败时保留错误与重试入口。
- 移除业务窗口左上角重复显示的页面标题，同时保留平台窗口控制。
- 修正邮件模板字段布局和联系人候选补全体验。

### 变更

- macOS 发布产物拆分为 Intel x64 与 Apple Silicon arm64，不再构建通用包。
- 发布产物统一使用正式应用名 `NextMail`。

## [0.2.0] - 2026-08-04

### 新增

- 增加账户隔离的本地联系人、联系人工作区、联系人搜索、详情、编辑和最近往来。
- 收信时自动从邮件地址头创建联系人，并对历史邮件执行可恢复的联系人回填。
- 邮件列表、邮件详情和 Composer 支持联系人姓名优先、身份 Tag、名片悬浮、复制和编辑入口。
- Composer 增加账户内联系人候选；联系人设置支持自动加载更多。
- 邮件模板支持 To/Cc/Bcc、邮件标题和正文的逐字段非空覆盖。

### 变更

- 新增联系人和模板收件人数据迁移，继续保持账户隔离和既有安全边界。

## [0.1.1] - 2026-08-02

### 修复

- 修复发布构建中邮件正文的字体、颜色、背景、图片和表格样式异常，恢复与调试模式一致的邮件保真度。
- 调整发布态 CSP 样式策略，使经过 Rust 清洗的邮件 HTML 能保留必要的样式，同时不放宽脚本和邮件 sandbox 安全边界。

## [0.1.0] - 2026-08-01

### 新增

- 首个公开预览版本，提供账户配置、多账户、IMAP 本地阅读、SMTP 发件、草稿、回复/转发和持久化发送队列。
- 提供离线 SQLite 邮件视图、头部优先同步、按需正文、可选全文同步、搜索、文件夹管理和通知。
- 提供安全清洗的 HTML/CSS 邮件阅读、远程图片控制、CID 图片、附件下载/打开/另存为和原始邮件查看。
- 提供模板、签名、富文本 Composer、源码/预览、内嵌图片、主题/语言偏好及 Windows/macOS 窗口体验。
- 建立只在 `v*` tag 推送时触发的 GitHub Actions 三平台发布流程，并补充中英文项目 README。

### 修复

- 收口首次预览版的 BMP 内联图片、草稿关闭确认、Drafts 定向刷新、回复/转发结构、窗口标题、Composer 布局和独立邮件预览问题。
- 增加严格的邮件 HTML/CSS 清洗、sandbox/CSP、远程图片和附件内容校验边界。
